let express,mongoose,userRoutes;_367‍.w("express",[["default",["express"],function(v){express=v}]]);_367‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_367‍.w("./routes/userRoutes",[["userRoutes",["userRoutes"],function(v){userRoutes=v}]]);



const URL = "mongodb://0.0.0.0:27017/parcelkoi";
const options = {};
const app = express();

//middleware array
app.use(express.json());

userRoutes(app);

const PORT = process.env.PORT || 8080;
(async function () {
  try {
    await mongoose.connect(URL, options);
    app.listen(PORT, () => console.log(`SERVER IS RUNNING ON ${PORT}`));
    console.log(`Database is stablish`);
  } catch (error) {
    _367‍.g.console.log(error);
  }
})();
