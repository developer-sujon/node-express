let Schema,model;_d14‍.x([["User",()=>User]]);_d14‍.w("mongoose",[["Schema",["Schema"],function(v){Schema=v}],["model",["model"],function(v){model=v}]]);

const userSchema = new Schema(
  {
    username: {
      type: String,
      unique: true,
      required: true,
    },
  },
  { timestamps: true },
);

       const User = model("User", userSchema);
